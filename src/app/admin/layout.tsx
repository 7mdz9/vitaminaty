import type { ReactNode } from "react";

// TODO(M2): Admin chrome with sidebar, header, and MFA gate.
export default function AdminLayout({ children }: Readonly<{ children: ReactNode }>) {
  return children;
}
